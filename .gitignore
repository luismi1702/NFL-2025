# Caches de datos regenerables (generar_caches.py / Manning_bot.py / contenders_tracker.py)
pbp_cache/
__pycache__/
*.pkl

# Outputs generados en lote
draft_calendar/
kw_frames/
salidas/

# PNGs generados en la raiz (modo --raiz). Los definitivos van a salidas/,
# que tambien esta ignorado: son regenerables desde el script.
/*.png
!logos/*.png

# Metadatos del modelo (temporada con la que se entreno)
*.meta

# Capturas de referencia (nombres GUID de Windows)
{*.png

# Personal - nunca commitear
docs/superpowers/
_preview/

# PDF combinado de previas (regenerable con Previas.py modo jornada)
/previas_semana_*.pdf
